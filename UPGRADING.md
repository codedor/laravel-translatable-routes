# Upgrading

## From v1 to v2

- Install `wotz/laravel-translatable-routes` instead of `codedor/laravel-translatable-routes`
- Replace all occurrences of `Codedor\TranslatableRoutes` namespace with new `Wotz\TranslatableRoutes` namespace
